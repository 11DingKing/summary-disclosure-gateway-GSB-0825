# SQLite path is resolved by the Prisma CLI relative to prisma/schema.prisma,
# so "file:./dev.db" points at prisma/dev.db — the same file the server falls
# back to when DATABASE_URL is unset.
DATABASE_URL="file:./dev.db"

# Optional server binding.
# HOST=127.0.0.1
# PORT=3000
# LOG_LEVEL=info
