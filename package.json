{
  "name": "summary-disclosure-gateway",
  "version": "1.0.0",
  "description": "Source-attribution gateway for reading products: keeps source excerpts, reader notes, editor summaries and AI summaries unmistakably distinct.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "build": "tsc",
    "start": "prisma migrate deploy && node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --import tsx --test test/unicode.test.ts test/overlapping-citations.test.ts test/hash-invalidation.test.ts test/concurrent-publish.test.ts test/historical-snapshot.test.ts",
    "seed": "tsx prisma/seed.ts",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@fastify/swagger": "^8.15.0",
    "@fastify/swagger-ui": "^4.1.0",
    "@prisma/client": "^5.22.0",
    "fastify": "^4.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
